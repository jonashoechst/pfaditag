title: Pfadi Tag
author: VCP Hessen
email: hessen@vcp.de
description: >-
  Der PfadiTag ist eine Aktion, um in das Pfadfinder-sein reinzuschnuppern. 
  Die Aktion wird vom Verbande Christlicher Pfadfinder*innen in Hessen und der daran teilnehmenden Stämmen ausgerichtet.
baseurl: "" # the subpath of your site, e.g. /blog
url: "" # your site's origin, e.g. https://forestry.io

# Build settings
defaults:
  - scope:
      path: "" # an empty string here means all files in the project
    values:
      layout: "default" # this ensures all content renders with layouts/default.html

plugins:
  - jekyll-feed
  - jekyll-menus
# Exclude from processing.
# The following items will not be processed, by default. Create a custom list
# to override the default setting.
# exclude:
#   - Gemfile
#   - Gemfile.lock
#   - node_modules
#   - vendor/bundle/
#   - vendor/cache/
#   - vendor/gems/
#   - vendor/ruby/
